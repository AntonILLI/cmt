//navigation bar slide animation HOC Trigger connects with slide animation with children components
//Hoc -> high order component 
import React from "react";

const AbsoluteWrapper = ({ children }) => {
  return <>{children}</>;
};

export default AbsoluteWrapper;
